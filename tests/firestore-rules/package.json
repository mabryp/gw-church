{
  "name": "gw-church-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Exercises firestore.rules against the Firestore emulator. See wiki/wednesday-dinner-poll.md.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project gw-church --config ../../firebase.json \"node test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0"
  }
}
